// const timer = (setTime) => {
//   const intervalId = setInterval(() => {
//     setTime((prev) => prev + 1);
//   }, 1000);

//   return () => clearInterval(intervalId);
// };

// export default timer;
